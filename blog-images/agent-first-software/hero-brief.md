# Rust service-first hero

Status: generated successfully using the built-in image_gen tool, visually verified, and integrated as the article hero and sharing image. Current asset: `hero-chat2db-ui-plugins.png`. All three crab emblems were replaced with the official purple/green Chat2DB logo using the built-in image_gen edit tool. Original retained as `hero-rust-ui-plugins.png`.

## Source grounding

Read-only inspection of `/Users/ryan/mycode/Chat2DB-permission-aware-chatbi` found a Java/Spring backend and React client, not a Rust implementation. The hero depicts the requested future Rust direction, not a screenshot or claim that UI plugins are already implemented.

- `TrustedDataAccessController`: service token plus signed subject assertion, subject-context resolution, query submission and asynchronous query status.
- `PermissionDomainServiceImpl`: database-scoped permission domains, revision checks, publishing and disabling.
- `Chat2dbMcpConfiguration`: shared tools exposed through a Spring AI MCP adapter.

## Art direction

Original anime science-fiction key visual, wide 16:9. Warm cream lighting, slate/navy shadows, cyan connections, rust-orange accents. A young adult engineer in a practical navy utility jacket plugs a feather-thin translucent UI cartridge into a massive orange-and-graphite service engine. The foundation occupies most of the visual mass. Three removable UI panels show abstract charts, chat, and settings; their physical connectors make the plugin idea immediately readable. A small robot Agent connects directly to a dedicated service port. Identity, shield, database and audit motifs are integrated into the foundation. A small orange crab emblem evokes Rust. Restrict text to RUST CORE and UI PLUGINS. No diagram boxes, arrows, paragraphs, watermark or screenshot. Preserve safe margins for blog and sharing crops.

## Integration after visual verification

Save hero artwork beside this brief, set the article frontmatter image to it, and place it at the beginning of the article. Preserve the existing three explanatory diagrams and the article body.
